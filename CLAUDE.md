# CLAUDE.md

このファイルは Claude Code がこのプロジェクトで作業する際の前提・ルールを定義します。
Claude Code はセッション開始時にこのファイルを自動で読み込みます。

---

## 1. プロジェクト概要

<!-- プロジェクトの目的を1〜2行で。詳細は requirements.md に書く -->

- **プロジェクト名**: ○○○
- **目的**: ○○○を解決するための○○○
- **主な利用者**: ○○○

---

## 2. 仕様書の構成と参照順序

このプロジェクトは **仕様書駆動開発** を採用します。
作業前に必ず以下を参照してください。

| ファイル | 役割 | 参照タイミング |
| --- | --- | --- |
| `docs/requirements.md` | 要求書（WHAT / WHY） | 新機能の検討・仕様判断時 |
| `docs/design.md` | 設計書（HOW） | 実装方針・技術判断時 |
| `docs/plan.md` | 実行計画書（WHEN / STEPS） | タスク着手・進捗確認時 |

### 参照順序の原則

1. まず `requirements.md` を読み、**何を・誰のために作るか**を確認する
2. 次に `design.md` を読み、**どう作るか**の方針を確認する
3. 最後に `plan.md` を読み、**次にやるべきタスク**を確認する

**迷ったら上流（要求書）に戻る**こと。設計や実装で判断に迷う場合、要求書に立ち返って確認してください。

---

## 3. 仕様書の更新ルール

仕様書とコードのズレを防ぐため、以下のタイミングで必ず仕様書を更新してください。

- **新しい要求・仕様変更が発生したとき** → `requirements.md` を更新
- **設計方針・技術選定が変わったとき** → `design.md` を更新し、変更理由を「設計判断の記録」セクションに追記
- **タスク着手時・完了時** → `plan.md` のチェックボックスとステータスを更新
- **実装と仕様書がズレたとき** → どちらが正か判断し、仕様書側を先に更新してからコードを修正

仕様書にない勝手な判断（ライブラリ追加・アーキテクチャ変更など）は行わず、**先に仕様書の更新を提案**してください。

---

## 4. 作業開始時の手順

新しいタスクに着手するとき、Claude Code は以下の手順で動いてください。

1. `docs/` 配下の仕様書3点を読む
2. `plan.md` から次に着手すべきタスクを特定する
3. そのタスクに関連する要求書・設計書の該当箇所を確認する
4. 不明点・前提が曖昧な点があれば、実装前にユーザーに確認する
5. 作業方針を簡潔に示してから実装に入る

---

## 5. 技術スタック・ディレクトリ構成

<!-- プロジェクトに合わせて記述。Claude Codeが勝手に別技術を選ばないよう明示する -->

### 技術スタック

- 言語:
- フレームワーク:
- データベース:
- その他:

### ディレクトリ構成

```
./
├── CLAUDE.md           # このファイル
├── docs/
│   ├── requirements.md # 要求書
│   ├── design.md       # 設計書
│   └── plan.md         # 実行計画書
├── src/                # 実装コード
└── tests/              # テストコード
```

---

## 6. コーディング規約

<!-- 必要に応じて追記 -->

- 命名規則:
- コメント方針:
- テスト方針:
- コミットメッセージ:

---

## 7. 禁止事項

以下は **明示的に許可されるまで行わないでください**。

- 設計書に記載のない依存パッケージの追加
- 設計書に記載のないアーキテクチャ変更
- 仕様書を更新せずに仕様に関わる実装を進めること
- `plan.md` にないタスクへの着手（発見した場合はタスクとして提案のみ）
- 既存ファイルの大規模なリファクタリング（ユーザーの明示的な指示がある場合を除く）

---

## 8. コミュニケーションルール

- 回答は日本語で行う
- 実装方針は着手前に簡潔に共有する
- 不確実な箇所は「不明点」として明示し、推測で進めない
- 複数の選択肢がある場合は選択肢を提示してユーザーに判断を委ねる

---

## 9. 備考

<!-- プロジェクト固有の注意事項、過去の判断の経緯などを必要に応じて記述 -->

